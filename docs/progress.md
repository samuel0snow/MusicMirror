# 全栈链路推进记录

## 2026-09-16：范围与设计核对

- 已阅读 docs 中的架构、算法、接口、隐私说明，以及 design/项目完成指南.md、输入数据设想.png。
- 本次交付：可启动后端、持久化采集任务与数据、标准化、六大指数、快照/历史/对比/洞察，以及微信原生请求服务和按钮处理器。
- 按用户要求不制作 GUI、不测试真实账号、不引入 Docker；依赖下载使用 http://127.0.0.1:7897。
- 后端选择 Fastify + TypeScript。本地使用 Node 内置 SQLite 与持久化任务队列，避免先安装 PostgreSQL/Redis；保留存储和任务边界，后续生产化迁移。
- 以指南 MVP 为基础，补齐三次快照趋势及数据删除。曲风、语言等只展示可靠元数据；年度报告、相似歌手图谱和首次收听增强留待真实接口验证。
- 设计图中的近期红心不等同于近期播放：喜欢列表只用于收藏一致度，没有收藏时间时不推断一周内收藏。
- 当前用户已把设计文档移至 docs/design，保留该调整并更新引用。

## 验证计划

1. 数学性质与边界：均匀/极端分布、协作歌手权重、JSD、空数据、异常计数和缺失字段。
2. 本地假上游验证真实适配器：认证、采集字段、失败降级、重试、缓存与批处理，绝不调用真实账号。
3. 持久化和 API 集成：认证隔离、任务去重、失败状态、重启恢复、三个快照、对比、趋势、解绑与删除。
4. GUI 按钮处理器通过兼容 wx.request 的测试传输调用真实本地 HTTP 服务。
5. 类型检查、完整测试、构建和编译产物启动检查。

## 目标更新：先统一指导文档

- 用户新增宣传方向，并明确双模块；已阅读并建立 product-spec.md。
- 优先完善架构、算法、隐私文件，新增 roadmap.md，统一数据库、容器化及后续产品增强方向。
- 当前代码已有契约、算法、收藏对照、上游适配、标准化与仓储；服务入口、任务、按钮及验证仍待完成，尚不视为全栈完成。

## 2026-09-17：链路实现与验证

- 已接通服务入口、认证、持久化任务、标准化、六指数、双模块快照、历史/对比/趋势、解绑/删除。
- 小程序目录改为miniprogram，提供wx.request客户端与完整按钮处理器；没有创建GUI。
- 已核对上游公开文档：POST也须在URL添加timestamp以避免URL缓存混淆；Cookie仍只放body。代码和合成契约测试覆盖该约束。
- 首轮25项测试定位到真实HTTP下DELETE空body不应附JSON内容类型的问题，已修复；随后25项全过，类型检查和构建通过。
- 继续补验独立长期模块的数据不足解释、演示解绑状态、真实适配器连接本地HTTP假上游，以及版本/趋势边界；未调用任何真实账号。

## 最终验收记录

环境：Windows PowerShell，Node v25.9.0，npm 11.12.1。依赖通过7897代理安装，锁文件已生成；本次安装输出无已知漏洞。

| 用户要求 / 设计链路 | 实现与证据 |
| --- | --- |
| 依据docs先统一设计 | product-spec、architecture、algorithm-spec、api-contract、privacy已完善，并保留原始设计材料 |
| 两个独立输入模块 | Snapshot.modules含长期/近期收藏；profile.test及buttons.test验证独立权重、来源、空态、收藏变化与对照 |
| 可运行全栈 | Fastify认证/接口→任务→采集→标准化→算法→SQLite→报告；api.test与upstream-http.test验证 |
| GUI按钮连接API，不制作前端 | client.js与actions.js；buttons.test用实际本地HTTP完成按钮链路，无WXML/WXSS页面 |
| 六大指数与数据不足 | math/profile测试覆盖均匀、集中、重复/异常次数、协作艺人、元数据缺失、近期模式与置信度 |
| 快照/比较/趋势 | 三次快照、重复数据复用、旧快照不变、版本隔离与少于三点降级均有测试 |
| 持久化与恢复 | persistence.test关闭重开SQLite、恢复processing任务、保留会话/输入/密钥，并验证事务和TTL |
| 用户隐私 | API测试验证越权、过期/注销、加密Cookie、安全错误、解绑、删除取消任务与关联清理 |
| 不测试真实账号 | mock fixture、函数假上游和127.0.0.1假上游；只读取了公开上游文档 |
| 不先做Docker等封装 | npm直接启动，SQLite自动建表；PostgreSQL/Redis/容器化在roadmap列出迁移时机 |
| 记录推进和优化点 | 本文持续记录；roadmap列出SQL选型、队列、容器、认证、GUI、校准与审美增强 |
| 编译后可独立启动 | scripts/smoke.mjs启动dist服务器，实测health→登录→分析→双模块→删除，再退出并清理临时数据库 |

验证命令：`npm run check`，包含类型检查、27项自动测试、TypeScript构建和编译产物smoke。所有测试均使用独立临时数据库，不写入用户真实数据。

明确留待后续：真实账号字段/权限核对，近期红心时间与自动排序来源验证，二维码登录与GUI制作，生产PostgreSQL/Redis、容器化及部署，真实人群校准和未经验证的音乐特征。当前输入2可用显式选择跑通；时间未知如实保留，不伪称自动获取一周内收藏。

## 2026-09-17：首次真实测试准备（新的授权范围）

用户请求首次真实账号测试并要求轻量二维码方案。已复用Enhanced API内置二维码图片，增加扫码会话、查询凭证、状态轮询、过期/取消和微信动作；修正原适配器将800系列业务状态误判为服务错误的问题。

31项自动回归、构建与启动验证通过。本地真实服务已启动，实际二维码生成与等待/过期轮询可用；账号尚未扫码，数据采集结论待验证，详见real-account-test.md。此前全栈阶段的“仅mock验证”记录保留为当时事实。
