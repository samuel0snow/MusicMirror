# 音乐小程序：个人音乐行为观察系统 — 项目完成指南

> **文档版本**：v1.0  
> **更新日期**：2026-09-16  
> **项目定位**：基于网易云音乐数据构建“个人音乐行为观察系统（Personal Music Behavior Observatory）”，重点解释用户**如何喜欢音乐、偏好如何形成、近期如何变化**，而不是复刻“年度听歌报告”。

---

## 目录

1. [项目目标与产品边界](#1-项目目标与产品边界)
2. [与网易云年度报告的核心差异](#2-与网易云年度报告的核心差异)
3. [总体系统架构](#3-总体系统架构)
4. [数据源与网易云 API 设计](#4-数据源与网易云-api-设计)
5. [数据采集流程](#5-数据采集流程)
6. [标准化数据模型](#6-标准化数据模型)
7. [数学特征层](#7-数学特征层)
8. [六大核心行为指数](#8-六大核心行为指数)
9. [事实指标与分布指标](#9-事实指标与分布指标)
10. [Snapshot 与时间变化检测](#10-snapshot-与时间变化检测)
11. [洞察生成系统](#11-洞察生成系统)
12. [评分归一化与校准](#12-评分归一化与校准)
13. [缺失数据、异常数据与置信度](#13-缺失数据异常数据与置信度)
14. [数据库设计](#14-数据库设计)
15. [后端服务设计](#15-后端服务设计)
16. [前端 / 小程序信息架构](#16-前端--小程序信息架构)
17. [缓存、限流与任务系统](#17-缓存限流与任务系统)
18. [隐私、安全与合规建议](#18-隐私安全与合规建议)
19. [测试方案](#19-测试方案)
20. [开发阶段与验收标准](#20-开发阶段与验收标准)
21. [MVP、V1、V2 功能范围](#21-mvpv1v2-功能范围)
22. [项目目录建议](#22-项目目录建议)
23. [关键伪代码](#23-关键伪代码)
24. [算法示例](#24-算法示例)
25. [产品文案原则](#25-产品文案原则)
26. [长期演进方向](#26-长期演进方向)
27. [最终完成检查清单](#27-最终完成检查清单)
28. [参考资料](#28-参考资料)

---

# 1. 项目目标与产品边界

## 1.1 一句话定位

**看见你的音乐偏好是如何形成和变化的。**

系统不是简单展示“最常听的 100 首歌”，而是把用户的播放、收藏、歌手、专辑、曲风、近期行为等数据组织成可解释的行为模型。

核心问题包括：

- 用户的播放是否高度集中在少数歌曲？
- Top100 虽然有 100 首，但“有效曲库”实际相当于多少首？
- 用户主要是“喜欢具体歌曲”，还是会深入听歌手、专辑？
- 用户的音乐世界到底有多宽，而不仅是“歌手数量多不多”？
- 最近是在延续长期偏好，还是进入新阶段？
- 最近出现了哪些新的核心歌曲、歌手、专辑或曲风？
- 收藏/红心所表达的“主观选择”，和实际频繁播放的“真实行为”一致吗？
- 某个偏好是长期留存，还是短期高强度循环？

## 1.2 明确不做的事情

本系统原则上不做：

- 根据听歌行为推断人格、心理状态、智力、社会身份等。
- 使用“专情型人格”“念旧的人”“孤独型听众”等无法由数据直接支持的标签。
- 把 0–100 分解释成好坏、优劣或品味高低。
- 直接复制网易云年度报告已有的“年度最爱歌曲/歌手”等内容作为产品核心。
- 将第三方 API 的非稳定字段视为永远可靠的强依赖。

## 1.3 产品输出分三类

### A. 客观事实

例如：

- Top100 总播放：18,426 次
- 有效曲库：27.4 首
- 核心歌手：6 位
- Top10 播放占比：61.2%
- Top100 红心歌曲：73 首

### B. 数据分布

例如：

- 歌手播放份额
- 专辑播放份额
- 曲风分布
- 语言分布
- 年代分布

### C. 解释性指数

最终只保留 6 个核心指数：

1. **Concentration / 集中度**
2. **Deep Listening / 深听度**
3. **Breadth / 广度**
4. **Exploration / 探索度**
5. **Stability / 稳定度**
6. **Intent Alignment / 收藏—实播一致度**

每个指数必须可展开查看“为什么”。

---

# 2. 与网易云年度报告的核心差异

| 年度报告典型逻辑 | 本项目逻辑 |
|---|---|
| 今年听了什么 | 你是怎样听音乐的 |
| 一年一次 | 可持续更新 |
| Top 歌曲、Top 歌手 | 集中度、有效曲库、深听结构 |
| 风格展示 | 风格多样性与风格漂移 |
| 某个时间段的总结 | 多时间点的偏好轨迹 |
| 娱乐叙事 | 可解释分析 |
| 静态快照 | Snapshot 序列 + 变化检测 |
| “你最喜欢 X” | “X 在你的偏好结构中承担什么角色” |

网易云音乐 NodeJS API Enhanced 当前文档还提供 `/summary/annual` 年度听歌报告接口，并说明目前支持 2017–2024 年。项目可以把它视为**对照数据源**，但不应围绕它重新制作同类报告。

---

# 3. 总体系统架构

```text
网易云 API
   │
   ├── 播放记录
   ├── 最近播放
   ├── 红心 / 喜欢
   ├── 歌单
   ├── 歌曲详情
   ├── 歌手 / 相似歌手
   ├── 曲风
   └── 可选百科 / 回忆坐标
          │
          ▼
┌─────────────────────────────┐
│      Raw Data 原始数据层     │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│   Normalize 数据标准化层     │
│ Song / Artist / Album / ... │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│   Mathematical Features     │
│ HHI / Entropy / ESS / JSD   │
│ Top-K / Overlap / Coverage  │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│     Behavioral Metrics      │
│ 6 个核心行为指数             │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│ Snapshot + Change Detection │
└─────────────────────────────┘
          │
          ▼
┌─────────────────────────────┐
│ Insight / Visualization     │
│ 洞察、趋势、可解释性         │
└─────────────────────────────┘
```

推荐原则：

- **原始 API 响应永久与算法结果分离。**
- 算法只依赖标准化后的内部数据模型。
- 每次算法升级都记录 `algorithm_version`。
- Snapshot 保存“当时的事实”，避免今天重算后篡改历史。

---

# 4. 数据源与网易云 API 设计

API 参考：网易云音乐 NodeJS API Enhanced 文档。

> 注意：该项目属于第三方非官方接口实现。文档明确提醒不要轻易使用不可信公开服务，避免账号与密码泄露。建议自行部署 API 服务，仅保存必要 Cookie/会话信息，并限制日志中的敏感字段。

## 4.1 核心必需接口

### 4.1.1 用户播放记录 `/user/record`

目标：长期 Top100、周播放结构。

建议获取：

- `type=0`：长期 / 全部数据视图（实际字段以接口返回为准）
- `type=1`：周数据视图

项目使用：

- 长期核心歌曲
- 长期播放次数
- 周期短期偏好
- 长期与近期重合

强依赖等级：**P0**

### 4.1.2 最近播放 `/record/recent/song`

文档说明默认返回数量为 100，可通过 `limit` 控制。

项目使用：

- 最近歌曲集合
- 新鲜度
- 长期 Top100 之外的探索比例
- 最近核心歌曲识别

强依赖等级：**P0**

### 4.1.3 歌曲详情 `/song/detail`

文档明确说明：支持多个音乐 ID，以逗号分隔；返回歌曲详情，`dt` 为歌曲时长。

项目使用：

- 标准歌名
- songId
- 歌手数组
- 专辑信息
- 时长
- 热度等可选字段

强依赖等级：**P0**

## 4.2 收藏 / 喜欢数据

### `/likelist` 或批量喜欢状态接口

用途：

- Top100 红心率
- 播放加权红心率
- 收藏—实播一致度

强依赖等级：**P0/P1**

具体实现应以部署版本实际可用接口为准。如果批量判断接口不稳定，应优先一次获取用户喜欢歌曲 ID 集合，再在本地做 Set 判断。

## 4.3 用户歌单

### `/user/playlist`

获取用户歌单列表。

### `/playlist/detail`

文档提醒：`tracks` 可能不完整，但 `trackIds` 完整，可使用完整 `trackIds` 再调用 `/song/detail`。

### `/playlist/track/all`

文档说明：可获取歌单全部歌曲，并支持 `limit`、`offset` 分页。

项目使用：

- 主动整理行为
- 用户创建歌单与收藏歌单区分（需结合返回字段）
- 歌单—播放一致度
- 核心歌曲是否被用户主动纳入个人集合

强依赖等级：**P1**

## 4.4 歌手相关

### `/artist/detail`

歌手详情。

### `/artist/album`

歌手专辑。

### `/artist/top/song`

歌手热门 50 首。

### `/simi/artist`

文档明确：传入歌手 ID 获取相似歌手。

项目使用：

- 歌手标准信息
- 艺人生态图谱
- 艺人生态跨度

强依赖等级：

- 歌手基本信息：**P1**
- 相似歌手：**P2**

> `/simi/artist` 反映平台提供的相似关系，不等于严格音乐学上的风格距离。因此只用于辅助计算“生态相似度”，不应作为唯一内容分类依据。

## 4.5 曲风相关

文档存在曲风相关接口，如：

- `/style/list`
- `/style/detail`
- `/style/song`
- `/style/album`
- `/style/artist`
- 以及部署版本中可用的用户曲风偏好接口

用途：

- 曲风分布
- 曲风多样性
- 长期 vs 近期曲风漂移
- 平台偏好信号与实际播放的交叉验证

强依赖等级：**P2**

原因：歌曲到曲风的映射是否能稳定、完整获得，需要在实际部署环境中验证。

## 4.6 可选百科 / 回忆坐标

如果部署版本支持并返回稳定字段，可接入：

- 歌曲百科
- 语言
- 首次收听时间
- 回忆坐标

用途：

- 语言分布
- 首次收听年龄
- 播放密度
- 长期留存
- 新欢强度

强依赖等级：**P3 / 实验性**

**原则：MVP 不依赖这些字段。**

## 4.7 年度报告 `/summary/annual`

文档说明支持获取当前用户年度听歌报告，目前支持 2017–2024 年。

用途：

- 只做对照和展示补充
- 不参与核心算法
- 可用于验证某些年度统计与本系统长期结构是否存在明显偏差

强依赖等级：**P3**

---

# 5. 数据采集流程

## 5.1 首次完整分析

```text
用户授权 / Cookie 可用
       │
       ▼
获取账户基础信息
       │
       ├── /user/record?type=0
       ├── /user/record?type=1
       ├── /record/recent/song
       ├── 获取喜欢歌曲 ID 集合
       └── /user/playlist
       │
       ▼
合并所有涉及 songId
       │
       ▼
批量 /song/detail
       │
       ▼
标准化 Song / Artist / Album
       │
       ├── 可选：artist detail / simi artist
       ├── 可选：style
       └── 可选：wiki / first listen
       │
       ▼
计算数学特征
       │
       ▼
计算六大行为指数
       │
       ▼
生成 Snapshot
       │
       ▼
生成洞察
```

## 5.2 后续刷新

后续刷新不要每次完整重抓所有内容。

推荐：

1. 拉取最新 `/user/record`。
2. 拉取最近播放。
3. 获取喜欢歌曲列表版本。
4. 新出现的 songId 才请求 `/song/detail`。
5. 已有 artist/album/song 元数据优先读缓存。
6. 重新计算指标。
7. 创建新 Snapshot。
8. 与最近一个可比 Snapshot 做差异分析。

## 5.3 抓取幂等性

每次采集创建：

```text
collection_run_id
user_id
started_at
finished_at
api_version
status
raw_checksum
```

同一批数据重复采集时，可通过 checksum 判断是否需要创建新的指标快照。

---

# 6. 标准化数据模型

## 6.1 SongFeature

```ts
interface SongFeature {
  songId: string;
  name: string;

  artists: Array<{
    artistId: string;
    name: string;
  }>;

  album?: {
    albumId: string;
    name: string;
  };

  durationMs?: number;
  publishTime?: number;

  longPlayCount: number;
  weekPlayCount: number;

  recentPlayCount?: number;
  appearedInRecent: boolean;

  liked: boolean | null;
  playlistCount?: number;

  styleIds?: string[];
  language?: string | null;
  firstListenAt?: number | null;

  metadataConfidence: number;
}
```

## 6.2 ArtistFeature

```ts
interface ArtistFeature {
  artistId: string;
  name: string;

  top100SongCount: number;
  longPlayCount: number;
  weekPlayCount: number;

  likedSongCount: number;
  albumCountInTop100: number;

  playShare: number;
  songShare: number;

  similarityEdges?: Array<{
    targetArtistId: string;
    weight: number;
  }>;
}
```

## 6.3 AlbumFeature

```ts
interface AlbumFeature {
  albumId: string;
  name: string;
  artistIds: string[];

  songCountInTop100: number;
  longPlayCount: number;
  weekPlayCount: number;
  playShare: number;

  publishYear?: number;
}
```

## 6.4 Snapshot

```ts
interface MusicProfileSnapshot {
  snapshotId: string;
  userId: string;
  createdAt: string;

  dataWindow: {
    longRecordAvailable: boolean;
    weeklyRecordAvailable: boolean;
    recentRecordSize: number;
  };

  facts: Record<string, number | string | null>;
  distributions: Record<string, unknown>;
  features: Record<string, number | null>;
  indexes: Record<string, number | null>;
  confidence: Record<string, number>;

  algorithmVersion: string;
}
```

---

# 7. 数学特征层

这一层不直接展示“结论”，只做标准数学计算。

设 Top100 中第 `i` 首歌曲播放次数为：

\[
x_i
\]

总播放：

\[
X=\sum_{i=1}^{N}x_i
\]

播放概率：

\[
p_i=\frac{x_i}{X}
\]

其中 `N <= 100`，如果用户实际不足 100 首，按实际 N 处理。

## 7.1 Top-K Share

\[
C_k=\sum_{i=1}^{k}p_i
\]

建议计算：

- C1
- C3
- C5
- C10
- C20
- C50

## 7.2 HHI / Simpson Concentration

\[
HHI=\sum_i p_i^2
\]

标准化集中度：

\[
HHI_{norm}=\frac{HHI-1/N}{1-1/N}
\]

范围约为 `[0,1]`。

## 7.3 Effective Song Size（有效曲库）

\[
ESS=\frac{1}{\sum_i p_i^2}
\]

解释：如果用户的实际播放分布等价于均匀播放 27.4 首歌，则 ESS = 27.4。

归一化：

\[
ESS_{norm}=\frac{ESS}{N}
\]

## 7.4 Shannon Entropy

\[
H=-\sum_i p_i\ln p_i
\]

归一化：

\[
H_{norm}=\frac{H}{\ln N}
\]

用于歌手、专辑、曲风、语言、年代等多样性分析。

## 7.5 Gini（辅助，不进入核心评分首选）

Gini 可作为可解释辅助指标，但建议不要与 HHI 同时大量进入综合评分，否则重复计算集中性。

## 7.6 Weighted Coverage

例如红心歌曲的播放加权覆盖：

\[
Coverage_{liked}=\frac{\sum_{i \in liked}x_i}{\sum_i x_i}
\]

## 7.7 Jaccard / Overlap

长期核心集合 `A` 与近期核心集合 `B`：

\[
J(A,B)=\frac{|A\cap B|}{|A\cup B|}
\]

核心留存率：

\[
Retention(A,B)=\frac{|A\cap B|}{|A|}
\]

## 7.8 Jensen-Shannon Divergence（JSD）

对长期分布 `P` 与近期分布 `Q`：

\[
M=\frac{P+Q}{2}
\]

\[
JSD(P,Q)=\frac12 KL(P\|M)+\frac12 KL(Q\|M)
\]

使用 `log2` 时结果位于 `[0,1]`。

用途：

- 歌手漂移
- 曲风漂移
- 语言漂移
- 年代漂移

需要做零概率平滑，例如 `epsilon = 1e-12`。

---

# 8. 六大核心行为指数

所有 0–100 指数都必须：

- 是描述，不是评分好坏。
- 页面可以展开显示组成因子。
- 保存 raw feature 与 normalized score。
- 指数算法版本化。

---

## 8.1 Concentration / 集中度

### 回答的问题

**“你的播放有多集中在少数核心歌曲？”**

### 输入

- HHI_norm
- C5
- C10
- ESS_norm

### 推荐初始公式

先得到子特征：

\[
f_1=HHI_{norm}
\]

\[
f_2=C_{10}
\]

\[
f_3=1-ESS_{norm}
\]

最终：

\[
ConcentrationRaw=0.45f_1+0.35f_2+0.20f_3
\]

最终分数不要简单 `raw * 100`，推荐在真实用户样本积累后使用百分位映射，见第 12 章。

### 展开解释

展示：

- Top1 / Top5 / Top10 占比
- ESS
- HHI

### 注意

C10 与 HHI 有相关性，因此权重不要过高叠加。

---

## 8.2 Deep Listening / 深听度

### 回答的问题

**“你是否会深入一个歌手或一张专辑，而不只是听单曲？”**

### 歌手深听度

歌手 `a` 在 Top100 中有 `n_a` 首歌曲。

定义深度函数：

\[
d(n)=\frac{\ln(1+n)}{\ln(1+n_{cap})}
\]

建议 `n_cap = 10`，超过 10 按 1 处理。

歌手深听度：

\[
ArtistDepth=\sum_a p_a\cdot d(n_a)
\]

其中 `p_a` 是该歌手播放份额。

### 专辑深听度

同理：

\[
AlbumDepth=\sum_b p_b\cdot d(n_b)
\]

建议 `n_cap = 8`。

### 最终

\[
DeepListeningRaw=0.65ArtistDepth+0.35AlbumDepth
\]

原因：Top100 对歌手覆盖通常比对完整专辑覆盖更稳定。

### 联合艺人处理

一首歌多位歌手时推荐两套方案：

- **主方案：均分播放权重**，若 2 位歌手，则每位计 `x_i/2`。
- 备选：第一艺人视作主艺人。

建议全项目统一采用均分权重，避免合作歌曲重复放大总播放量。

---

## 8.3 Breadth / 广度

### 回答的问题

**“你的音乐世界分布得有多宽？”**

不是简单看歌手数量。

### 基础组成

推荐：

- Artist Diversity
- Album Diversity
- Style Diversity（有数据时）
- Artist Ecosystem Breadth（可选）

歌手多样性：

\[
D_A=\frac{-\sum_a p_a\ln p_a}{\ln N_A}
\]

专辑多样性：

\[
D_B=\frac{-\sum_b p_b\ln p_b}{\ln N_B}
\]

如果曲风数据可靠：

\[
D_S=\frac{-\sum_s p_s\ln p_s}{\ln N_S}
\]

### MVP 公式

无可靠曲风：

\[
BreadthRaw=0.7D_A+0.3D_B
\]

### V1 公式

有可靠曲风：

\[
BreadthRaw=0.50D_A+0.20D_B+0.30D_S
\]

### 艺人生态跨度（高级）

若相似歌手图谱可用：

\[
EcosystemBreadth=1-MeanSimilarity
\]

可作为单独事实展示，暂不强行进入核心 Breadth，直到相似度数据稳定。

---

## 8.4 Exploration / 探索度

### 回答的问题

**“最近有多大比例的播放走出了你的长期核心？”**

### 核心定义

长期集合：`LongTop100`

近期记录中的歌曲集合 / 序列：`Recent`

新鲜度：

\[
NoveltyRate=\frac{近期播放中不属于LongTop100的记录数}{近期播放记录总数}
\]

若最近接口只有去重列表而无事件级频次，则退化为：

\[
NoveltyUnique=\frac{|RecentUnique-LongTop100|}{|RecentUnique|}
\]

### 新歌手探索

\[
NewArtistRate=\frac{近期出现且长期核心未出现的歌手权重}{近期全部歌手权重}
\]

### 新专辑探索

同理。

### 推荐公式

\[
ExplorationRaw=0.60NoveltyRate+0.25NewArtistRate+0.15NewAlbumRate
\]

若只有歌曲级数据：仅使用 `NoveltyRate`，同时降低置信度。

### 不要做

不要用“Top100 尾部很多歌”来直接推断探索度。那只是长期分布形状，不等于近期探索行为。

---

## 8.5 Stability / 稳定度

### 回答的问题

**“近期偏好与长期核心有多一致？”**

### 核心留存

长期核心建议定义为 LongTop20：

\[
CoreRetention=\frac{|LongTop20\cap RecentCore20|}{|LongTop20|}
\]

### 歌手分布稳定

\[
ArtistStability=1-JSD(P^{artist}_{long},P^{artist}_{recent})
\]

### 曲风稳定

\[
StyleStability=1-JSD(P^{style}_{long},P^{style}_{recent})
\]

### 推荐公式

无曲风：

\[
StabilityRaw=0.55CoreRetention+0.45ArtistStability
\]

有可靠曲风：

\[
StabilityRaw=0.45CoreRetention+0.35ArtistStability+0.20StyleStability
\]

### Stability 与 Exploration 的区别

两者相关但不完全互为反数。

例：

- 用户仍大量听老核心，同时也增加很多新歌：稳定度可以高，探索度也可以高。
- 因此不要直接令 `Stability = 100 - Exploration`。

---

## 8.6 Intent Alignment / 收藏—实播一致度

### 回答的问题

**“你主动收藏的音乐，与真正反复播放的音乐有多一致？”**

### 喜欢歌曲覆盖率

数量：

\[
LikeHitRate=\frac{|Top100\cap Liked|}{|Top100|}
\]

播放加权：

\[
LikeWeighted=\frac{\sum_{i\in Liked}x_i}{\sum_i x_i}
\]

### 歌单覆盖（可选）

对于用户主动创建 / 维护的歌单集合 `PlaylistOwned`：

\[
PlaylistWeighted=\frac{\sum_{i\in PlaylistOwned}x_i}{\sum_i x_i}
\]

### 推荐公式

MVP：

\[
IntentAlignmentRaw=0.30LikeHitRate+0.70LikeWeighted
\]

V1 有高质量歌单数据：

\[
IntentAlignmentRaw=0.25LikeHitRate+0.55LikeWeighted+0.20PlaylistWeighted
\]

播放加权比例权重更高，因为系统关注真实行为。

---

# 9. 事实指标与分布指标

以下不要强行做成 0–100 分。

## 9.1 首页关键事实

建议：

- Top100 总播放次数
- 有效曲库 ESS
- 不同歌手数量
- 不同专辑数量
- 核心歌曲数
- 核心歌手数
- Top10 占比
- 红心覆盖率

## 9.2 核心定义建议

### 核心歌曲

不要使用单一规则。

推荐满足任一条件：

- 排名前 20；或
- 播放份额 >= 2%；或
- 播放次数 >= 中位数的 2.5 倍。

最终 `core_song` 标志应保存触发原因。

### 核心歌手

推荐：

- 播放份额 >= 5%；或
- Top100 中至少 4 首歌且播放份额 >= 3%。

阈值后续用真实样本校准。

## 9.3 分布

建议输出：

- Top 10 歌曲份额图
- Top 10 歌手份额图
- Top 专辑份额
- 曲风分布
- 年代分布
- 语言分布

所有分布都优先按**播放次数加权**，并可切换“歌曲数量视图”。

---

# 10. Snapshot 与时间变化检测

这是产品长期价值的核心。

## 10.1 Snapshot 创建策略

建议：

- 用户手动刷新时：若数据有变化则创建。
- 自动刷新：每日最多 1 个正式 Snapshot。
- 用户无变化时：记录采集日志但不必生成重复 Snapshot。

## 10.2 Snapshot 不可变

每个 Snapshot 保存：

- 原始事实摘要
- 分布
- 六大指数
- 子特征
- 数据完整度
- 算法版本

算法升级后：

- 可以基于原始标准化数据生成 `recomputed_snapshot`
- 不覆盖旧算法版本结果

## 10.3 变化检测

比较 `S_t` 与 `S_{t-1}`。

### 新核心歌曲

```text
currentCoreSongs - previousCoreSongs
```

### 退出核心歌曲

```text
previousCoreSongs - currentCoreSongs
```

### 新核心歌手

同理。

### 指数变化

例如：

```text
ΔConcentration = 78 - 62 = +16
```

只有超过噪声阈值才生成洞察。

建议初始阈值：

- 核心指数：绝对变化 >= 8 分
- 单个份额：变化 >= 5 个百分点
- 排名：移动 >= 10 位

后续根据真实波动校准。

## 10.4 趋势

至少 3 个 Snapshot 才允许描述趋势。

例如：

- 连续 3 次 Concentration 上升
- 连续 3 次某歌手份额上升

不要仅用两个点写“持续上升”。

## 10.5 状态机（可选）

可以定义**行为状态**而不是人格类型：

- 稳定核心期
- 探索扩张期
- 偏好迁移期
- 高集中循环期
- 均匀分散期

状态必须由明确规则生成。

示例：

```text
if Stability >= 70 && Exploration < 35:
    state = 稳定核心期
elif Exploration >= 65 && Stability >= 55:
    state = 探索扩张期
elif Stability < 40 && DriftHigh:
    state = 偏好迁移期
```

页面必须解释状态规则，避免神秘标签。

---

# 11. 洞察生成系统

洞察不要让大模型直接“看数据自由发挥”。

推荐两阶段：

```text
算法事实
  ↓
规则引擎生成 Insight Objects
  ↓
模板 / LLM 仅负责语言润色
```

## 11.1 Insight Object

```ts
interface Insight {
  id: string;
  type: string;
  priority: number;
  confidence: number;

  title: string;
  evidence: Array<{
    metric: string;
    value: number | string;
    comparison?: number | string;
  }>;

  templateKey: string;
  params: Record<string, unknown>;
}
```

## 11.2 示例

数据：

```text
Top10 Share = 0.64
ESS = 24.8
上期 Top10 Share = 0.49
```

规则：

```text
if top10Share >= 0.6 && essNorm <= 0.35:
    insight = HIGH_CONCENTRATION
```

文案：

> 你的播放正在向少数核心歌曲集中。当前 Top10 贡献了 64% 的 Top100 播放量，有效曲库约为 25 首。

不写：

> 你是一个非常专一的人。

## 11.3 洞察优先级

推荐：

1. 显著变化
2. 高置信异常结构
3. 长期稳定特征
4. 普通事实

首页最多 3–5 条洞察，避免报告过载。

---

# 12. 评分归一化与校准

## 12.1 MVP 阶段

没有大规模用户数据前，不要假装“78 分意味着超过 78% 用户”。

可以使用固定数学映射：

```text
score = clamp(round(raw * 100), 0, 100)
```

但 UI 文案要写“指数值”，不要写“超过 X% 用户”。

## 12.2 样本积累后

推荐转为经验分布百分位：

\[
Score(x)=100\cdot F(x)
\]

其中 `F(x)` 是基于合格用户样本的经验 CDF。

这样 80 表示该行为特征位于样本较高区间。

## 12.3 样本分层

归一化样本最好考虑：

- Top100 实际歌曲数
- 总播放量
- 账号活跃程度

避免新用户和十年老用户直接不加处理地混合校准。

## 12.4 版本化

```text
concentration_v1_fixed
concentration_v2_percentile_2027Q1
```

每个 Snapshot 必须记录版本。

---

# 13. 缺失数据、异常数据与置信度

## 13.1 总体置信度

每个指数都输出：

```text
value: 74
confidence: 0.91
```

## 13.2 置信度来源

建议：

\[
Confidence = C_{coverage}\times C_{sample}\times C_{freshness}\times C_{metadata}
\]

### Coverage

所需字段可用比例。

### Sample

数据量是否充分。

例如 Top100 实际只有 12 首，则多样性指标可信度降低。

### Freshness

近期数据是否足够新。

### Metadata

歌手、专辑、曲风等是否完整。

## 13.3 最低样本建议

- Concentration：至少 10 首歌曲
- Deep Listening：至少 10 首 + 3 位歌手
- Breadth：至少 15 首
- Exploration：近期至少 20 条或 20 首有效记录
- Stability：长期 + 近期均可用
- Intent Alignment：喜欢列表成功获取

## 13.4 播放次数异常

需要处理：

- 0 次
- 负数
- null
- 重复 songId
- 超大异常值

原则：

- 负数/null 视为无效。
- 重复 songId 聚合。
- 超大值不随意截断，先记录异常；集中度本来就应该反映高播放。

## 13.5 云盘歌曲

文档说明 `/song/detail` 某些云盘歌曲可能只能得到有限信息。

处理：

- 播放行为仍计入歌曲层指标。
- 缺失 artist/album 时，不进入对应维度分母，或放入 `unknown`。
- Breadth 等指标同时输出 metadata coverage。

---

# 14. 数据库设计

推荐 PostgreSQL。

## 14.1 users

```sql
CREATE TABLE users (
  id BIGSERIAL PRIMARY KEY,
  netease_uid VARCHAR(64) UNIQUE NOT NULL,
  nickname TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

## 14.2 songs

```sql
CREATE TABLE songs (
  song_id VARCHAR(64) PRIMARY KEY,
  name TEXT NOT NULL,
  album_id VARCHAR(64),
  duration_ms INTEGER,
  publish_time BIGINT,
  raw_metadata JSONB,
  metadata_updated_at TIMESTAMPTZ
);
```

## 14.3 artists

```sql
CREATE TABLE artists (
  artist_id VARCHAR(64) PRIMARY KEY,
  name TEXT NOT NULL,
  raw_metadata JSONB,
  metadata_updated_at TIMESTAMPTZ
);
```

## 14.4 song_artists

```sql
CREATE TABLE song_artists (
  song_id VARCHAR(64) NOT NULL,
  artist_id VARCHAR(64) NOT NULL,
  artist_order INTEGER NOT NULL DEFAULT 0,
  PRIMARY KEY (song_id, artist_id)
);
```

## 14.5 albums

```sql
CREATE TABLE albums (
  album_id VARCHAR(64) PRIMARY KEY,
  name TEXT,
  publish_time BIGINT,
  raw_metadata JSONB
);
```

## 14.6 collection_runs

```sql
CREATE TABLE collection_runs (
  id UUID PRIMARY KEY,
  user_id BIGINT NOT NULL,
  started_at TIMESTAMPTZ NOT NULL,
  finished_at TIMESTAMPTZ,
  status VARCHAR(32) NOT NULL,
  api_version TEXT,
  raw_checksum TEXT,
  error_summary TEXT
);
```

## 14.7 playback_records_summary

```sql
CREATE TABLE playback_records_summary (
  run_id UUID NOT NULL,
  user_id BIGINT NOT NULL,
  song_id VARCHAR(64) NOT NULL,
  long_play_count INTEGER NOT NULL DEFAULT 0,
  week_play_count INTEGER NOT NULL DEFAULT 0,
  long_rank INTEGER,
  week_rank INTEGER,
  PRIMARY KEY (run_id, song_id)
);
```

## 14.8 user_song_state

```sql
CREATE TABLE user_song_state (
  run_id UUID NOT NULL,
  user_id BIGINT NOT NULL,
  song_id VARCHAR(64) NOT NULL,
  liked BOOLEAN,
  appeared_recently BOOLEAN NOT NULL DEFAULT FALSE,
  recent_weight DOUBLE PRECISION,
  playlist_count INTEGER,
  first_listen_at TIMESTAMPTZ,
  PRIMARY KEY (run_id, song_id)
);
```

## 14.9 snapshots

```sql
CREATE TABLE snapshots (
  id UUID PRIMARY KEY,
  user_id BIGINT NOT NULL,
  collection_run_id UUID NOT NULL,
  created_at TIMESTAMPTZ NOT NULL,
  algorithm_version TEXT NOT NULL,
  facts JSONB NOT NULL,
  distributions JSONB NOT NULL,
  features JSONB NOT NULL,
  indexes JSONB NOT NULL,
  confidence JSONB NOT NULL
);
```

## 14.10 insights

```sql
CREATE TABLE insights (
  id UUID PRIMARY KEY,
  snapshot_id UUID NOT NULL,
  type TEXT NOT NULL,
  priority INTEGER NOT NULL,
  confidence DOUBLE PRECISION NOT NULL,
  evidence JSONB NOT NULL,
  payload JSONB NOT NULL
);
```

## 14.11 API 原始响应

是否长期保存原始 JSON 取决于隐私策略。

建议：

- 开发环境：可短期保存调试。
- 生产：只保留必要字段，原始 Cookie 永不进入通用日志。
- 如果保存 raw response，应加密并设置 TTL。

---

# 15. 后端服务设计

推荐技术栈：

- Node.js 20+
- TypeScript
- NestJS / Fastify（二选一）
- PostgreSQL
- Redis
- BullMQ（任务队列）
- Zod / class-validator 做返回结构校验

## 15.1 服务拆分

```text
AuthService
NeteaseApiService
CollectorService
MetadataService
FeatureService
MetricService
SnapshotService
InsightService
ReportService
```

### NeteaseApiService

只负责：

- 调用 Enhanced API
- 超时
- 重试
- 限流
- schema validate
- cache

不能在此层写算法。

### CollectorService

负责：

- 编排采集流程
- 合并 songId
- 判断增量元数据

### FeatureService

负责数学基础特征。

### MetricService

负责 6 大指数。

### SnapshotService

负责冻结一次分析结果。

### InsightService

负责差异分析与洞察对象。

## 15.2 自己的前端 API

### POST `/analysis/refresh`

触发刷新。

返回：

```json
{
  "runId": "...",
  "status": "processing"
}
```

### GET `/analysis/latest`

返回最新 Snapshot。

### GET `/analysis/history`

返回时间序列。

### GET `/analysis/snapshot/:id`

返回完整快照。

### GET `/analysis/metric/:metricKey`

返回某个指数的解释数据。

例如：

```json
{
  "key": "concentration",
  "value": 78,
  "confidence": 0.95,
  "factors": {
    "top10Share": 0.61,
    "effectiveSongSize": 29.4,
    "hhiNormalized": 0.18
  }
}
```

---

# 16. 前端 / 小程序信息架构

## 16.1 首页

不做“100 个数字大屏”。

建议：

### 顶部

```text
你的音乐状态
最近更新：今天 14:32
```

### 核心事实卡

- 有效曲库 27 首
- Top10 占 61%
- 核心歌手 6 位

### 六维行为图

可以使用雷达图，但必须同时提供文本数值，避免雷达图难以准确读取。

```text
集中度       78
深听度       84
广度         61
探索度       46
稳定度       72
收藏一致度   89
```

### 本次最值得注意的 3 个变化

例如：

- 某歌手播放份额从 8% 升至 19%
- Top10 集中度明显上升
- 最近 32% 的歌曲不在长期 Top100

## 16.2 “我的结构”页

- 单曲集中曲线
- Top 歌手
- Top 专辑
- 有效曲库
- 深听结构

## 16.3 “我的偏好”页

- 曲风
- 语言
- 年代
- 歌手生态

没有可靠数据的模块直接隐藏，不要猜。

## 16.4 “我的变化”页

核心产品页：

```text
过去 30 天
过去 90 天
全部 Snapshot
```

展示：

- 六指标趋势
- 新核心歌曲
- 新核心歌手
- 退出核心
- 偏好漂移

## 16.5 指数详情页

每个指数：

1. 分数
2. 一句话解释
3. 为什么
4. 构成指标
5. 历史趋势
6. 算法说明

必须做到可解释。

---

# 17. 缓存、限流与任务系统

Enhanced API 文档说明项目本身存在缓存机制，示例文档提到缓存约 2 分钟，并指出频繁请求可能导致 IP 高频问题。

因此自己的后端还应再做一层保护。

## 17.1 Redis TTL 建议

| 数据 | TTL |
|---|---:|
| song detail | 30 天 |
| artist detail | 30 天 |
| album metadata | 30 天 |
| user record | 10–30 分钟 |
| recent songs | 5–10 分钟 |
| likelist | 30 分钟 |
| playlist list | 30 分钟 |
| style metadata | 7 天 |

## 17.2 请求去重

相同用户短时间点击刷新：

```text
analysis-lock:{userId}
```

若已有采集任务运行，不再创建第二个。

## 17.3 重试

推荐指数退避：

```text
1s -> 2s -> 4s
```

最多 3 次。

认证失效不无限重试。

## 17.4 元数据批处理

`/song/detail` 支持多个 ID，应批量调用。

例如：

- 每批 50–100 个（实际最大稳定量通过测试确定）
- 不逐首发请求

---

# 18. 隐私、安全与合规建议

## 18.1 Cookie

- 服务端加密存储。
- 不写入普通日志。
- 不发送给前端分析页面。
- 提供用户主动解除绑定。
- 最小权限原则。

## 18.2 第三方公开 API

Enhanced 文档明确提醒：不要轻易信任他人的公开服务，以免泄露账号和密码。

**生产环境应自行部署。**

## 18.3 数据删除

建议支持：

- 删除所有 Snapshot
- 删除网易云绑定
- 删除缓存元数据中的用户关联
- 删除采集原始数据

公共 song/artist 元数据可以与用户数据解耦。

## 18.4 日志脱敏

禁止记录：

- 完整 Cookie
- CSRF Token
- 登录二维码敏感会话
- 任何可直接复用认证信息

## 18.5 用户文案

明确说明：

- 指数描述的是听歌数据结构，不评价音乐品味。
- 某些数据受网易云接口能力、可见范围和采样窗口影响。

---

# 19. 测试方案

## 19.1 数学单元测试

必须覆盖：

### 全部均匀

```text
100 首，每首 10 次
```

预期：

- ESS = 100
- HHI_norm ≈ 0
- Concentration 低

### 单首极端

```text
第一首 10000 次，其余很少
```

预期：

- ESS 接近 1
- Concentration 高

### 歌手深听

```text
Top100 主要由 3 位歌手，各大量作品
```

Deep Listening 高。

### 单曲散点

```text
80 位歌手，每位 1–2 首
```

Breadth 较高、Deep Listening 较低。

## 19.2 JSD 测试

- 相同分布 → JSD = 0
- 完全不重叠分布 → 接近最大值

## 19.3 API Contract Test

对每个核心接口保存 fixture：

```text
tests/fixtures/netease/user-record.json
```

API 返回结构变化时能够第一时间发现。

## 19.4 Snapshot 回归测试

固定一组输入：

```text
fixture_user_A
```

算法升级前后比较结果。

如果变化超过预期，必须人工确认。

## 19.5 数据不完整测试

- 没有周数据
- 没有红心权限
- 专辑为空
- 云盘歌曲
- 用户 Top100 不足 100 首

系统不能崩溃，应降级输出。

---

# 20. 开发阶段与验收标准

## 阶段 0：API 可行性验证

目标：确定真实接口字段。

完成：

- `/user/record`
- `/record/recent/song`
- `/song/detail`
- 喜欢列表
- 用户歌单

验收：

- 用 3–5 个不同账号跑通。
- 保存字段样本。
- 明确哪些接口必须登录。
- 明确隐私/权限问题。

## 阶段 1：数据层

完成：

- 标准化模型
- 数据库
- Collector
- 缓存
- 去重

验收：

任一用户刷新后可生成完整标准化 SongFeature 列表。

## 阶段 2：数学特征

完成：

- Top-K
- HHI
- ESS
- Entropy
- Overlap
- JSD

验收：数学 fixture 全通过。

## 阶段 3：六大指数 MVP

先实现：

- Concentration
- Deep Listening
- Breadth
- Exploration
- Stability
- Intent Alignment

验收：

每个指标都返回：

```text
value
raw
confidence
factors
algorithmVersion
```

## 阶段 4：Snapshot

完成：

- 创建
- 历史列表
- 对比
- 变化检测

验收：

同用户至少 3 次 Snapshot 可正常生成趋势。

## 阶段 5：前端 MVP

完成：首页、结构页、变化页、详情页。

## 阶段 6：曲风/语言/回忆坐标增强

只有接口验证稳定后再加入。

---

# 21. MVP、V1、V2 功能范围

## MVP

强烈建议只做：

- Top100
- 最近播放
- 歌曲详情
- 喜欢列表
- 歌手 / 专辑聚合
- 六大指数
- Snapshot
- 两次 Snapshot 对比

**不要一开始做语言、歌词语义、AI 心情分析等。**

## V1

增加：

- 歌单一致度
- 曲风
- 年代
- 3+ Snapshot 趋势
- 变化洞察
- 艺人生态跨度实验版

## V2

增加：

- 首次收听 / 回忆坐标（接口稳定时）
- 播放密度
- 长期留存
- 用户自定义观察周期
- 年度报告对照
- 更高级的音乐图谱

---

# 22. 项目目录建议

```text
music-observer/
├── apps/
│   ├── miniapp/
│   └── api/
│
├── packages/
│   ├── contracts/
│   ├── algorithms/
│   ├── insight-engine/
│   └── ui-shared/
│
├── services/
│   └── netease-api-enhanced/
│
├── docs/
│   ├── algorithm-spec.md
│   ├── api-contract.md
│   └── privacy.md
│
├── tests/
│   ├── fixtures/
│   ├── algorithms/
│   └── integration/
│
└── infra/
    ├── docker-compose.yml
    └── migrations/
```

`packages/algorithms` 必须尽可能是纯函数，不依赖数据库和 HTTP。

---

# 23. 关键伪代码

## 23.1 播放概率

```ts
function normalizePlayCounts(items: { playCount: number }[]) {
  const total = items.reduce((s, x) => s + Math.max(0, x.playCount), 0);
  if (total === 0) return items.map(() => 0);
  return items.map(x => Math.max(0, x.playCount) / total);
}
```

## 23.2 ESS

```ts
function effectiveSize(probabilities: number[]) {
  const sumSq = probabilities.reduce((s, p) => s + p * p, 0);
  return sumSq > 0 ? 1 / sumSq : 0;
}
```

## 23.3 Entropy

```ts
function normalizedEntropy(probabilities: number[]) {
  const p = probabilities.filter(x => x > 0);
  if (p.length <= 1) return 0;

  const h = -p.reduce((s, x) => s + x * Math.log(x), 0);
  return h / Math.log(p.length);
}
```

## 23.4 JSD

```ts
function jsd(p: number[], q: number[]) {
  const eps = 1e-12;
  const P = p.map(x => Math.max(x, eps));
  const Q = q.map(x => Math.max(x, eps));
  const M = P.map((x, i) => (x + Q[i]) / 2);

  const kl = (A: number[], B: number[]) =>
    A.reduce((s, x, i) => s + x * Math.log2(x / B[i]), 0);

  return 0.5 * kl(P, M) + 0.5 * kl(Q, M);
}
```

实际使用前必须保证 `P/Q` 使用同一类别空间并重新归一化。

## 23.5 歌手聚合

```ts
for (const song of songs) {
  const artists = song.artists;
  const share = 1 / Math.max(artists.length, 1);

  for (const artist of artists) {
    artistPlay[artist.id] += song.playCount * share;
    artistSongSet[artist.id].add(song.songId);
  }
}
```

## 23.6 Snapshot 对比

```ts
function compareSnapshots(prev, curr) {
  return {
    indexDelta: diffIndexes(prev.indexes, curr.indexes),
    newCoreSongs: setDiff(curr.coreSongs, prev.coreSongs),
    lostCoreSongs: setDiff(prev.coreSongs, curr.coreSongs),
    newCoreArtists: setDiff(curr.coreArtists, prev.coreArtists),
    distributionShift: {
      artist: jsd(prev.artistDist, curr.artistDist),
      style: maybeJsd(prev.styleDist, curr.styleDist),
    }
  };
}
```

---

# 24. 算法示例

假设某用户：

```text
Top100 总播放 = 10,000
Top10 = 6,200
HHI_norm = 0.31
ESS = 24
不同歌手 = 31
Top100 红心歌曲 = 68
红心歌曲贡献播放 = 8,700
最近 100 条中，34 条不属于长期 Top100
```

## 24.1 集中度输入

```text
C10 = 0.62
ESS_norm = 24 / 100 = 0.24
1 - ESS_norm = 0.76
HHI_norm = 0.31
```

Raw：

\[
0.45(0.31)+0.35(0.62)+0.20(0.76)
=0.5085
\]

MVP 固定映射约 51。

注意：这不代表“只算中等集中”。真实 percentile 校准后分数可能明显不同，所以前期必须显示组成因子。

## 24.2 收藏一致度

```text
LikeHitRate = 0.68
LikeWeighted = 0.87
```

\[
AlignmentRaw=0.3(0.68)+0.7(0.87)=0.813
\]

MVP 指数约 81。

解释：

> 虽然 Top100 中只有约 68% 被红心，但这些歌曲贡献了 87% 的播放量，说明高频核心与主动收藏高度重合。

## 24.3 探索度

最近事件级数据中：

```text
NoveltyRate = 0.34
NewArtistRate = 0.28
NewAlbumRate = 0.31
```

\[
ExplorationRaw=0.6(0.34)+0.25(0.28)+0.15(0.31)=0.3205
\]

指数约 32（MVP 映射）。

---

# 25. 产品文案原则

## 25.1 描述行为，不推断人格

推荐：

> 最近的播放仍主要围绕长期核心歌曲展开。

不推荐：

> 你是一个念旧的人。

推荐：

> 最近 34% 的播放来自长期 Top100 之外。

不推荐：

> 你最近开始厌倦旧歌。

## 25.2 每个洞察最好附证据

```text
播放变得更集中
Top10：49% → 63%
有效曲库：41 → 27 首
```

## 25.3 不评价品味高低

禁止：

- 音乐品味高级
- 审美更好
- 小众所以更有品位

## 25.4 指数解释

“78”不能孤零零出现。

应该：

```text
集中度 78
较多播放集中于少数核心歌曲

Top10 占比 61%
有效曲库 27 首
```

---

# 26. 长期演进方向

## 26.1 自建历史会成为真正的数据壁垒

API 提供的是当前可见窗口，而项目自己的 Snapshot 会逐步形成：

\[
Profile_1, Profile_2, ..., Profile_n
\]

随着时间增长，系统可以回答：

- 哪些歌手从短期兴趣变成长期核心？
- 哪些歌只短暂高强度出现？
- 用户的音乐世界是在扩大还是收缩？
- 曲风是否发生长期迁移？
- 专辑型深听是否越来越明显？

## 26.2 “歌曲生命周期”

未来可为每首歌曲建立：

```text
发现期 → 快速增长期 → 核心期 → 稳定期 → 衰退期 → 回归期
```

前提是本项目自己持续采集 Snapshot，而不是依赖平台提供完整播放时间线。

## 26.3 偏好图谱

未来可形成多层图：

```text
User
 ├── Song
 ├── Artist
 │    └── Similar Artist
 ├── Album
 └── Style
```

可以研究：

- 核心簇
- 跨簇连接
- 新偏好从哪个旧偏好分支出现

## 26.4 个性化推荐（谨慎）

如果未来做推荐，建议与“分析”模块分开：

- 分析负责描述已发生行为。
- 推荐负责给出候选音乐。

不要让推荐算法反过来污染分析定义。

---

# 27. 最终完成检查清单

## 产品

- [ ] 项目一句话定位清晰
- [ ] 不复刻年度报告作为主要价值
- [ ] 六大指数均可解释
- [ ] 首页最多展示 3–5 条重点洞察
- [ ] 不使用人格化推断

## API

- [ ] 用户长期播放记录验证
- [ ] 周播放记录验证
- [ ] 最近播放验证
- [ ] song/detail 批量验证
- [ ] 喜欢列表验证
- [ ] 用户歌单验证
- [ ] API 失败有降级方案
- [ ] Cookie 不写日志

## 数据

- [ ] songId 去重
- [ ] 多歌手权重方案固定
- [ ] 云盘歌曲可降级
- [ ] 缺失 album 可处理
- [ ] Snapshot 不可变
- [ ] algorithm_version 落库

## 算法

- [ ] Top-K 测试
- [ ] HHI 测试
- [ ] ESS 测试
- [ ] Entropy 测试
- [ ] JSD 测试
- [ ] Concentration 可解释
- [ ] Deep Listening 可解释
- [ ] Breadth 可解释
- [ ] Exploration 有真实近期数据支持
- [ ] Stability 不直接等于 100 - Exploration
- [ ] Intent Alignment 使用播放加权
- [ ] 所有指数有 confidence

## Snapshot

- [ ] 首次分析能生成 Snapshot
- [ ] 第二次可比较
- [ ] 第三次开始可以描述趋势
- [ ] 无变化时避免重复 Snapshot
- [ ] 算法升级不覆盖旧历史

## 前端

- [ ] 首页有核心事实
- [ ] 六大指数有详情页
- [ ] 有历史变化页
- [ ] 分数都有“为什么”
- [ ] 缺失模块不会显示假数据

## 安全

- [ ] 自行部署 Enhanced API
- [ ] Cookie 加密
- [ ] 日志脱敏
- [ ] 有删除数据入口
- [ ] 有解绑入口

---

# 28. 参考资料

## 网易云音乐 NodeJS API Enhanced

文档主页：

https://docs-neteasecloudmusicapi.focalors.ltd/

本文档在 2026-09-16 参考其公开文档确认的能力包括但不限于：

- `/song/detail`：支持批量歌曲 ID，`dt` 表示歌曲时长。
- `/playlist/detail`：`tracks` 可能不完整，可利用完整 `trackIds` 再取歌曲详情。
- `/playlist/track/all`：获取歌单全部歌曲并支持分页。
- `/artist/detail`：歌手详情。
- `/artist/album`：歌手专辑。
- `/artist/top/song`：歌手热门歌曲。
- `/simi/artist`：相似歌手。
- `/record/recent/song`：最近播放歌曲，默认数量 100。
- 曲风相关接口：`/style/album`、`/style/artist` 等。
- `/summary/annual`：年度听歌报告，文档当前注明支持 2017–2024。
- API 自身存在缓存机制，文档提醒避免高频请求。
- 文档明确提醒不要轻易使用他人提供的公开服务，以避免账号和密码泄露。

> **重要**：第三方接口可能随网易云上游变化、项目版本变化而调整。正式开发前，务必对每个强依赖接口做 Contract Test，并保存真实响应 fixture。本文提出的可选接口（尤其曲风偏好、百科、语言、首次收听等）应先在目标部署版本中验证返回字段，再升级为正式数据依赖。

---

# 附录 A：推荐的算法配置文件

把所有阈值从代码中抽离：

```yaml
algorithmVersion: music-profile-v1

concentration:
  weights:
    hhi: 0.45
    top10: 0.35
    inverseEss: 0.20

artistDepth:
  cap: 10

albumDepth:
  cap: 8

deepListening:
  weights:
    artist: 0.65
    album: 0.35

breadth:
  noStyle:
    artist: 0.70
    album: 0.30
  withStyle:
    artist: 0.50
    album: 0.20
    style: 0.30

exploration:
  weights:
    songNovelty: 0.60
    newArtist: 0.25
    newAlbum: 0.15

stability:
  noStyle:
    coreRetention: 0.55
    artistStability: 0.45
  withStyle:
    coreRetention: 0.45
    artistStability: 0.35
    styleStability: 0.20

intentAlignment:
  likeOnly:
    hitRate: 0.30
    weighted: 0.70
  withPlaylist:
    hitRate: 0.25
    weighted: 0.55
    playlistWeighted: 0.20

insightThresholds:
  indexDelta: 8
  shareDeltaPercentagePoints: 5
  rankDelta: 10
```

这样后续校准无需修改算法代码。

---

# 附录 B：最推荐的 MVP 数据返回结构

```json
{
  "snapshotId": "uuid",
  "createdAt": "2026-09-16T08:00:00Z",
  "algorithmVersion": "music-profile-v1",
  "facts": {
    "topSongCount": 100,
    "totalTop100PlayCount": 18426,
    "effectiveSongSize": 27.4,
    "artistCount": 41,
    "albumCount": 63,
    "top10Share": 0.612,
    "likedSongRate": 0.73,
    "likedPlayWeightedRate": 0.89
  },
  "indexes": {
    "concentration": 78,
    "deepListening": 64,
    "breadth": 58,
    "exploration": 46,
    "stability": 72,
    "intentAlignment": 89
  },
  "confidence": {
    "concentration": 0.99,
    "deepListening": 0.95,
    "breadth": 0.91,
    "exploration": 0.82,
    "stability": 0.80,
    "intentAlignment": 0.98
  },
  "distributions": {
    "artists": [],
    "albums": []
  },
  "insights": []
}
```

---

# 附录 C：最终项目原则（建议写入 README）

1. **数据先于叙事。** 每条洞察必须能追溯到具体数字。
2. **行为不是人格。** 系统描述音乐行为，不推断人的本质。
3. **变化比排名更重要。** 项目核心价值来自 Snapshot 序列。
4. **实际播放优先。** 收藏、曲风偏好等属于辅助意图信号。
5. **可解释优先于复杂。** 一个用户能理解的 ESS，往往比黑盒模型更有产品价值。
6. **缺失就承认缺失。** 不为填满页面而猜测语言、曲风或心理。
7. **算法必须版本化。** 任何评分调整都可追溯。
8. **第三方接口不可假设永远稳定。** 核心接口必须 Contract Test。
9. **不做品味高低评分。** 六大指数是坐标，不是成绩。
10. **自建历史是长期壁垒。** 每一个可靠 Snapshot 都在增加未来的分析能力。

