<view class="screen {{theme}}"><mm-header back="{{true}}" bindback="back"/><view class="content"><mm-state busy="{{busy}}" error="{{error}}" bindretry="retry"/><view class="eyebrow">连接 / 由你决定</view><view class="title">只读你的音乐选择</view><view class="subtitle">读取可见播放与红心状态，保存报告以便比较。不会读取你的网易云密码。</view><view class="card"><view wx:if="{{status==='idle'}}"><mm-chart kind="prism"/><view class="heading">从一次授权开始</view><view>可随时解绑或删除 MusicMirror 数据。</view></view><block wx:else><image wx:if="{{qrImage}}" src="{{qrImage}}" class="qr" mode="aspectFit"/><view class="heading">{{status==='scanned'?'已扫码，请在网易云确认':status==='expired'?'二维码已过期':'请用网易云音乐 App 扫码'}}</view><view class="muted" wx:if="{{qrImage}}">有效期至 {{expiry}}</view></block></view><checkbox-group bindchange="consent"><label class="checkline"><checkbox value="yes" checked="{{consented}}" color="#6550b9"/><text>我已了解并同意本次数据使用说明</text></label></checkbox-group><button class="link" bindtap="go" data-url="/pages/privacy/index">阅读完整隐私说明</button><view class="actions"><button class="primary" bindtap="start" disabled="{{busy || !consented}}">{{status==='idle'?'同意并生成二维码':'重新生成二维码'}}</button><button class="secondary" bindtap="help">在同一部手机上操作？</button><view wx:if="{{help}}" class="note">可在电脑或另一台设备显示二维码，再用网易云扫码。客户端如支持相册识码，可使用系统截图后在网易云识别；识别方式由网易云客户端决定。</view><button class="link" bindtap="cancel">取消连接</button></view></view></view>
